<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.mr235.volleyimageloaderdemo"
          xmlns:android="http://schemas.android.com/apk/res/android">

	<uses-permission android:name="android.permission.INTERNET"/>

	<application
		android:allowBackup="true"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:supportsRtl="true"
		android:name=".application.DemoApplication"
		android:theme="@style/AppTheme">
		<activity android:name=".activity.MainActivity">
			<intent-filter>
				<action android:name="android.intent.action.MAIN"/>

				<category android:name="android.intent.category.LAUNCHER"/>
			</intent-filter>
		</activity>
		<activity android:name=".activity.ImageRequestActivity">
		</activity>
		<activity android:name=".activity.ImageLoaderActivity">
		</activity>
		<activity android:name=".activity.NetworkImageViewActivity">
		</activity>
		<activity android:name=".activity.RecyclerViewActivity">
		</activity>
	</application>

</manifest>
